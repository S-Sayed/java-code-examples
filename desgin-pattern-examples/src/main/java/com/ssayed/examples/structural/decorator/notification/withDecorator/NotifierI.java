package com.ssayed.examples.structural.decorator.notification.withDecorator;

// common interface created to support basic implementation and decorator objects
public interface NotifierI {

	void send(String message);
}
